"""P3 schemas: HTTP-facing projections of the service interfaces.

Only shapes that the existing P3 contract already fixes are modelled here. P2's
intelligence report body, P2 claim/intelligence fields and P1 evidence bundle internals are
NOT modelled: they travel through the API as opaque mappings until the P2 report schema is
frozen and integrated.
"""
