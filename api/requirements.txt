# TRACE investigator API - Person 3 (Investigator Experience & Integration).
# Deliberately minimal: web framework, ASGI server, multipart parsing, test client, test runner.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
pytest>=8.0
httpx>=0.27